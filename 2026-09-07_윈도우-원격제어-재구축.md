# 윈도우 원격제어 재구축 (2026-09-07)

> 8월에 맥으로 이관하며 껐던 윈도우 원격제어를 다시 세운 기록.
> 검증 환경: Windows 11 Pro (DESKTOP-PARKSH), 터미널 CLI 2.1.263, 데스크톱 앱 1.46388.4.
> 이 문서의 수치·경로는 전부 당일 실측값이다.

관련 문서: [2026-08-10_remote-control_정리.md](2026-08-10_remote-control_정리.md) — 8월 윈도우 운영 문서.
**§2·§5·§6의 `.vbs` 구성은 이 문서로 대체됐다.** 나머지(핀 관리 습관, `-c` 금지, 판별법)는 여전히 유효하다.

---

## 0. 한눈에 보기

| 항목 | 8월 상태 | 지금 |
|---|---|---|
| 시작프로그램 | `.vbs`가 프로젝트 9개를 한꺼번에 기동 | `.vbs` → `start-hidden.ps1` → **세션 1개**(AskMeAnything) |
| 세션 이름 | `AskMeAnything` 등 | `AskMeAnything 윈도우` |
| 이어받기 방식 | `--resume <고정 UUID>` 핀 | **핀 없음** — 매번 새 대화로 기동 |
| 앱 자동 실행 | 켜짐 | **꺼짐** (`ClaudeStartup`) |
| 터미널 CLI | 2.1.232 (8/14자) | **2.1.263**, 재로그인 완료 |
| 세션 간 대화 | 없음 | `talk` · `cross-talk` · `peer-talk` 설치 |

---

## 1. 근본 원인 — 터미널 CLI가 8/26부터 로그아웃돼 있었다

맥에서 이 PC로 계속 메시지를 보냈는데 `computer_unreachable`로 끝났다. 원인은 네트워크도 세션 ID도 아니었다.

```
~/.claude/.credentials.json
  claudeAiOauth.accessToken   = "" (0자)
  claudeAiOauth.refreshToken  = "" (0자)
  refreshTokenExpiresAt       = 2026-08-26 07:39   ← 이미 만료
```

이 상태에서 `claude remote-control`을 부르면 이렇게 끝난다:

```
Error: You must be logged in to use Remote Control.
```

**로그인이 없으면 remote-control 세션을 아예 만들 수 없다.** 8/13 맥 이관 후 이 PC의 터미널 CLI를 안 써서 아무도 몰랐다.

> ⚠️ **데스크톱 앱 로그인과 터미널 CLI 로그인은 별개다.** 앱은 멀쩡히 로그인돼 있었다.
> 앱 내장 CLI(`AppData\Roaming\Claude\claude-code\<버전>\claude.exe`)도 같은
> `.credentials.json`을 보므로 **앱이 로그인돼 있어도 터미널 쪽은 로그아웃일 수 있다.**

### 고친 순서

```bash
claude update                    # 2.1.232 -> 2.1.263
claude auth login --claudeai     # 브라우저에서 사용자가 승인
claude auth status               # loggedIn: true 확인
```

**`claude auth` 하위 명령은 2.1.232에는 없었다.** 갱신이 선행돼야 한다.
로그인 후 `refreshTokenExpiresAt`은 2026-10-06 — **한 달마다 만료되므로 주기적 재로그인이 필요하다.**

### 두 번째 관문 — 폴더 신뢰

로그인만으로는 안 뜬다. 처음 여는 폴더는 신뢰 확인이 필요하다.

```
Error: Workspace not trusted. Please run `claude` in <폴더> first
       to review and accept the workspace trust dialog.
```

`claude`를 창에서 한 번 실행해 수락해야 한다. **사용자가 직접 눌러야 하는 보안 확인이다.**

---

## 2. 새 자동 시작 구성

파일 2개다.

| 파일 | 역할 |
|---|---|
| `%APPDATA%\...\Startup\claude-remote-control.vbs` | 로그온 후 **90초 대기** → 아래 스크립트를 창 없이 실행 |
| `%USERPROFILE%\.claude\remote-control\start-hidden.ps1` | 중복 기동 방지 후 remote-control 세션 기동 |

저장소 사본은 [`setup/windows/`](setup/windows/)에 있다 (문서와 파일이 다르면 파일이 최신).

### 8월 `.vbs`와 달라진 점

**① 세션 이름의 한글을 `.ps1`로 옮겼다.** 8월 문서는 "`.vbs`는 UTF-16LE 인코딩 필수"라고 적어뒀는데, 그 제약은 `.vbs` 안에 한글 세션 이름이 있었기 때문이다. 지금 `.vbs`는 **ASCII만** 쓰므로 인코딩을 신경 쓸 필요가 없다.

**② 대신 `.ps1`에 BOM이 필요하다.** Windows PowerShell 5.1은 **BOM 없는 UTF-8 파일을 cp949로 읽는다.** 한글 세션 이름이 깨지므로 `start-hidden.ps1`은 **UTF-8 with BOM**으로 저장해야 한다.

**③ 중복 기동 가드가 들어갔다.** 같은 이름의 remote-control 프로세스가 이미 있으면 새로 띄우지 않는다. 8월 구성에서 "`.vbs` 래퍼 PowerShell이 살아 있으면 폴백 빈 세션을 띄운다"던 문제([8/10 문서 §6](2026-08-10_remote-control_정리.md))를 구조적으로 막는다.

**④ `--resume` 핀을 안 쓴다.** 8월 방식의 유일한 약점이 "대화를 새로 시작하면 `.vbs`의 UUID를 갱신해야 하고, 그걸 놓쳐 사고가 났다"([8/10 문서 §10](2026-08-10_remote-control_정리.md))는 것이었다. 지금은 핀을 없애 그 실패 모드 자체를 제거했다. **대신 재부팅하면 새 대화로 시작한다** — 이어받기가 필요하면 8월 방식을 되살려야 한다.

### 수동 기동·중지

```bash
powershell -ExecutionPolicy Bypass -File "%USERPROFILE%\.claude\remote-control\start-hidden.ps1"
```

중지는 `start-hidden.ps1` 맨 아래 주석의 명령을 쓴다.

### 끄려면

`claude-remote-control.vbs` → `claude-remote-control.vbs.disabled` 로 이름만 바꾼다.

---

## 3. 정리한 것 / 일부러 안 건드린 것

### 껐다

- **`ClaudeStartup`** (스토어 앱 시작 등록) `State 2` → `1`.
  레지스트리 `HKCU\Software\Classes\Local Settings\...\AppModel\SystemAppData\Claude_pzs8sxrjxfjjc\ClaudeStartup`.
  ⚠️ **클래식 `Run` 키와 시작프로그램 폴더만 봐서는 안 보인다.** MSIX 앱은 여기에 등록된다.
- **8월 `.vbs`** → `%USERPROFILE%\.claude\remote-control-archive\`로 이동 (9개 세션 일괄 기동용, 보관)
- **`bridge-session` 포인터 106줄** — 옛 대화 9개에서 제거. 앱이 뜰 때마다 8월 세션에 자동 재연결하던 것.
  전량 백업: `~/.claude/backups/bridge-strip-20260907-145345/` (24MB). 대화 내용은 한 줄도 안 건드렸다.

### 안 건드렸다

- **`CoworkVMService`** (표시명 "Claude", `StartMode=Auto`, LocalSystem) — 앱의 샌드박스 VM 관리자다.
  세션을 만들지 않고 **열어둔 포트도 없다.** 유휴 메모리 21MB. 끌 실익이 없다.
  굳이 끄려면 관리자 권한으로 `sc.exe config CoworkVMService start= demand`.
- **맥** — 사용자가 정리 지시에서 "맥은 빼고"라고 명시 제외했다. 맥에도 죽은 bridge 포인터 3개가 있지만
  (`AskMeAnything`, `morning-economy`, `rabbit-typing-adventure`) `run.sh`에 스냅샷 폴백이 있어 무해하다.
- **`lg Kafka` 세션** — 전 과정에서 접근 금지.

---

## 4. 세션 간 대화 (talk / cross-talk / peer-talk)

세 스킬을 설치했다. 원본은 `claude/askmenanything-cloud-jr8bzk` 브랜치의 `skills/`에 있다.

| 스킬 | 범위 | 설치 blob |
|---|---|---|
| `talk` | 통합 진입점 (`/talk whoami`, `/talk list`, `/talk <상대>`) | `3d03874` |
| `cross-talk` | 다른 컴퓨터·클라우드 | `608b473` |
| `peer-talk` | 같은 컴퓨터 | `ecff5a3` |

설치 위치: `%USERPROFILE%\.claude\skills\<이름>\SKILL.md`
설치는 `git show <커밋>:skills/<이름>/SKILL.md > <대상>`으로 하고, `git hash-object`로 blob 일치를 확인한다.

### 이 PC의 제약

**데스크톱 앱 세션에는 `SendMessage`가 없다.** 앱이 `--disallowedTools SendMessage`로 띄운다.
`ToolSearch`로도 못 되살린다. 그래서 1차 경로(ListAgents + SendMessage)를 쓸 수 없고,
**2차 경로(`RemoteTrigger` 트리거 중계)만 쓴다.**

### RemoteTrigger create body — 맥·윈도우가 각각 두 번씩 400을 맞은 지점

```json
{
  "name": "...", "persistent_session_id": "session_...",
  "job_config": { "ccr": {
    "environment_id": "env_...",
    "events": [{ "data": { "isSynthetic": true, "type": "user",
                           "uuid": "<UUID v4>",
                           "message": { "role": "user", "content": "<본문>" } } }],
    "session_context": { "allowed_tools": ["preset:default"] }
  }}
}
```

- `session_request.events`로 보내면 → `Extra inputs are not permitted`
- `environment_id`를 빼면 → `must set ccr.environment_id`
- `initiation`은 **선택사항** (없어도 200)
- `environment_id`는 `action:list`로 기존 루틴에서 그대로 가져오면 된다
- 순서: `create` → `run` → `update {"enabled": false}` (**`delete` 액션이 없다**)

---

## 5. 실측으로 확인된 사실

**① 브리지 세션 ID는 회전한다.** 데스크톱 앱 세션의 `bridgeSessionId`가 수십초~수분 단위로 바뀐다.
실측: `session_01Mbr8ka…` → `session_01ERZAmF…`. 세션 이름도 `askmeanything-65` → `-56` → `-e4`로 바뀌었다.
**미리 받아둔 ID는 발사 시점엔 낡아 있다** — 맥이 오후 내내 이 PC를 못 찾은 진짜 이유다.
현재 값은 `~/.claude/sessions/<pid>.json`에서 볼 수 있다.

**② 데스크톱 앱 세션도 메시지를 받는다.** 한때 "받을 수 없다"고 정리했으나 틀렸다.
브리지 ID로 보내면 정상 수신된다. 문제는 수신 불가가 아니라 **주소가 안정적이지 않다는 것**이다.

**③ `ListAgents`의 피어 이름은 `--name`과 다를 수 있다.**
`--name "AskMeAnything 윈도우"`로 띄운 세션이 목록엔 `my-first-game-maker`로 떴다.

**④ 피어 목록은 실시간이 아니다.** 종료한 세션이 한동안 그대로 남는다.

**⑤ 트리거 중계는 즉시가 아니다 — 약 80초.**
핑퐁 테스트 실측: 맥 발사 16:31:44 → 윈도우 도착 16:33:04. 중간(16:32:18) 확인 시엔 미도착이었다.
**발사 성공(HTTP 200)과 상대 도착은 별개다.** 수신 측은 한 턴 안에서 기다릴 수 없고(메시지가 별도 턴으로 들어온다),
몇 초 안 왔다고 실패로 판정하면 안 된다.

**⑥ 본문은 한글 300~400자 이내로 끊는다.** 한글이 `\uXXXX`로 이스케이프되며 **약 6배**로 불어나
JSON이 잘린다. 실패 사례가 5,194바이트였다.

**⑦ HTTPS 원격은 폐기 계정으로 인증된다.**
`git push` 시 `Permission to sanghyeon83/AskMeAnything.git denied to aiusertwo` → 403.
SSH 별칭으로 우회한다:

```bash
git push git@sanghyeon-github:sanghyeon83/AskMeAnything.git <브랜치>
```

영구 고정하려면 `git remote set-url --push origin git@sanghyeon-github:sanghyeon83/AskMeAnything.git`.
(페치는 HTTPS로 문제없이 된다.)

---

## 6. 점검 명령

```powershell
claude auth status
Get-CimInstance Win32_Process -Filter "Name='claude.exe'" | Where-Object { $_.CommandLine -match 'remote-control' } | Select-Object ProcessId, CreationDate
Test-Path "$env:APPDATA\Microsoft\Windows\Start Menu\Programs\Startup\claude-remote-control.vbs"
(Get-ItemProperty "HKCU:\Software\Classes\Local Settings\Software\Microsoft\Windows\CurrentVersion\AppModel\SystemAppData\Claude_pzs8sxrjxfjjc\ClaudeStartup").State
```

순서대로 **`loggedIn: true` / 프로세스 1개 / `True` / `1`** 이면 정상이다.

세션이 안 뜨면 **①번(로그인 만료)부터 의심할 것.** 오늘의 근본 원인이었다.
