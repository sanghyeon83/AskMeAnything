# 맥 환경 구축 정리 (2026-08-13)

> 윈도우 PC의 Claude 작업 환경(Remote Control 상시 세션 + 윈도우식 키보드 습관)을 맥북에 구축한 기록 — [윈도우 측 이관 인수인계서](2026-08-13_맥북-이관-인수인계.md)에 대한 맥 측 결과물이다.
> 새 맥에서 재현할 때는 이 문서를 위에서부터 순서대로 따라가면 된다.
> 실제 설정 파일은 이 저장소의 [`setup/`](setup/) 폴더에 있다 (문서와 파일이 다르면 파일이 최신).

관련 문서: [2026-08-10_remote-control_정리.md](2026-08-10_remote-control_정리.md) — 윈도우 구성 포함 전체 운영 문서. 이 문서는 "새 맥 셋업 절차" 관점으로만 정리했다.

> ⚠️ **2026-09-04 이후 달라진 것** (아래 본문에 반영했다):
> AskMeAnything_맥은 더 이상 launchd 상시 세션이 아니다. 데스크톱 앱 대화에서 `/remote-control` 을 켜는 방식으로 바꿨다.
> 그 과정에서 §2 「알아둘 제약」의 **"원격 노출하면 파일 패널을 잃는다"가 틀렸음이 확인**됐다 (§2 참고).
>
> ℹ️ **윈도우 구성은 2026-09-07에 재구축됐다.** 8월 `.vbs` 구성(세션 9개 일괄 기동, `--resume` 고정 핀)은 폐기됐다.
> 맥 구성 자체는 영향을 받지 않지만, 세션 간 대화(`talk`/`cross-talk`/`peer-talk`)의 실측값 — 중계 지연 약 80초,
> 한글 300~400자 한계, RemoteTrigger body 형태와 400 오류, 브리지 ID 회전 — 은 맥에도 그대로 적용된다.
> → [2026-09-07_윈도우-원격제어-재구축.md](2026-09-07_윈도우-원격제어-재구축.md)

---

## 0. 최종 상태 요약 (2026-08-13 기준)

- **Remote Control 상시 세션 7개**가 launchd로 자동 실행 중: Tokbell_맥, Tokbell Sender_맥, Morning Economy_맥, Rabbit Typing_맥, kafka test_맥, Tokbell HA_맥, Webs Madang_맥
  (2026-08-13 시점에는 AskMeAnything_맥을 포함해 8개였다. 9/4에 AskMeAnything_맥을 앱 세션으로 되돌리며 `run.sh`에서 주석 처리했다)
- 재부팅해도 **같은 세션·같은 대화**로 이어짐 (3단계 폴백 + 세션별 60초 자동 재시작)
- AskMeAnything_맥은 **데스크톱 앱 세션**이다 (2026-09-04 변경). 앱에서 열고 `/remote-control` 을 켜면
  파일 패널을 유지한 채 태블릿·폰에서도 열린다. 8월의 `--resume bd9cae22-…` 상시 노출 구성은 폐기했다 —
  CLI로 열면 파일 패널(소스코드)을 못 보기 때문이다
- Karabiner로 윈도우식 단축키 (Ctrl+C/V, 오른쪽 Alt=한/영, Ctrl+클릭=새 탭, Ctrl+±0=페이지 줌)
- Caps Lock = 대소문자 토글 (macOS 기본의 한/영 전환 기능 꺼둠)
- 크롬 Ctrl+휠 콘텐츠 확대: 확장 프로그램으로 해결
- `~/workspace`는 git 프로젝트 전용. 도구는 `~/Tools/`, 보관물은 `~/Archives/`

---

## 1. Claude CLI 설치 + 로그인

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

- PATH에 `~/.local/bin` 추가됨 (안 되면 `echo 'export PATH="$HOME/.local/bin:$PATH"' >> ~/.zshrc`)
- ⚠️ **데스크톱 앱 로그인과 CLI 로그인은 별개다.** 터미널에서 `claude` 실행 → 로그인 방법 선택에서 **1번(Claude 구독 계정)** Enter → 브라우저에서 승인
- Remote Control은 claude.ai 구독 계정으로만 된다

## 2. Remote Control 자동 시작 구성

윈도우의 `.vbs`+시작프로그램에 해당하는 macOS 구성. 파일 2개면 끝:

| 파일 | 위치 |
|---|---|
| [`setup/run.sh`](setup/run.sh) | `~/.claude/remote-control/run.sh` 로 복사 후 `chmod +x` |
| [`setup/com.sanghyeon.claude-remote-control.plist`](setup/com.sanghyeon.claude-remote-control.plist) | `~/Library/LaunchAgents/` 로 복사 |

```bash
mkdir -p ~/.claude/remote-control
cp setup/run.sh ~/.claude/remote-control/ && chmod +x ~/.claude/remote-control/run.sh
cp setup/com.sanghyeon.claude-remote-control.plist ~/Library/LaunchAgents/
launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.sanghyeon.claude-remote-control.plist
```

### 새 맥에서 반드시 먼저 할 것 (한 번씩만)

1. **폴더별 trust 승인**: 등록할 각 폴더에서 인터랙티브 `claude` 실행 → "Yes, I trust this folder" → `/exit`. 건너뛰면 백그라운드에서 `Workspace not trusted`로 조용히 죽는다
2. **최초 1회 동의 프롬프트**: 아무 폴더에서 `claude remote-control --spawn=same-dir --name "테스트"`를 창에서 실행 → `Enable Remote Control? (y/n)`에 **y** → 뜨는 것 확인 후 Ctrl+C. 이걸 안 하면 백그라운드 세션이 보이지 않는 프롬프트에서 영원히 대기한다
3. `run.sh`의 `RunRC` 줄들을 새 맥의 폴더 사정에 맞게 수정

### run.sh 동작 원리 (핵심만)

- `RunRC "폴더" "표시이름" "이어받기옵션"` 형식. 옵션: `""`(새 대화) / `"-c"`(이어받기) / `"--session-id <ID>"`(특정 세션 고정) / `"--resume <로컬UUID>"`(로컬 대화 상시 노출)
- `-c` 실패 시(4시간 넘게 꺼둔 경우 등) → `bridge-pointer.json`의 마지막 세션 ID로 재접속 → 그것도 실패하면 새 대화. **그래서 재부팅해도 세션 목록에 새 항목이 안 쌓인다**
- 세션이 죽으면 60초 후 자동 재시작. **완전히 멈추려면 kill이 아니라**: `launchctl bootout gui/$(id -u)/com.sanghyeon.claude-remote-control`
- 재시작: `launchctl kickstart -k gui/$(id -u)/com.sanghyeon.claude-remote-control`
- 로그: `~/.claude/remote-control/logs/`
- `--spawn=same-dir` 명시 필수 (안 하면 최초 실행 시 선택 프롬프트에서 백그라운드 대기)

### 데스크톱 앱 대화를 원격으로 넘기기 (필요할 때만)

> ⚠️ 2026-09-04 이후로는 **앱 세션에서 `/remote-control` 을 켜는 쪽이 낫다.** 아래 포크 방식은
> 요약본으로 복원돼 최근 맥락이 빠지고, 같은 대화를 두 프로세스가 열면 기록이 꼬인다.
> 실제로 이 방식으로 중복 세션 6개를 만들었다가 전부 정리한 적이 있다.

1. 1회 포크: `claude --resume <데스크톱대화UUID> --fork-session --remote-control "이름"` (⚠️ Claude 세션 안에서 실행하면 CLAUDE* 환경변수 때문에 대화 저장이 꺼진다 — 새 터미널에서 실행)
2. 생긴 포크본의 로컬 UUID를 `run.sh`에 `"--resume <UUID>"`로 등록
3. 이후 재부팅해도 같은 대화·같은 클라우드 세션 유지

### 알아둘 제약

- ⚠️ **아래 두 줄은 2026-09-04에 틀린 것으로 확인됐다.** 원문을 남기고 정정한다.
  - ~~"파일" 패널은 데스크톱 앱 로컬 세션 전용이고, 원격 세션에는 어느 기기에서 열어도 없다~~
  - ~~데스크톱 앱 로컬 대화는 모바일 목록에 안 뜬다~~
- **정정**: 파일 패널을 잃는 원인은 원격 노출이 아니라 **CLI로 여는 것**이었다.

  |  | 파일 패널(소스코드) | 태블릿·폰에서 열기 |
  |---|---|---|
  | 앱 세션 + `/remote-control` | ✅ | ✅ ← **권장** |
  | 앱 세션 (노출 안 함) | ✅ | ❌ |
  | CLI `--resume … --remote-control` | ❌ | ✅ |

  앱 세션에서 `/remote-control <이름>` 을 켜면 둘 다 가진다 (사용자 실측). CLI로 띄운 원격 세션에만
  "파일" 항목이 없다. 단 `/remote-control` 은 **사용자가 입력창에 직접 쳐야 한다** — 에이전트가 호출할 수단이 없다.

## 2-1. CLI 로그인 만료 감시 · 재로그인 자동 기동 (2026-09-08 기준)

**토큰 자체는 연장할 수 없다.** 실측(2026-09-07): 액세스 토큰은 갱신돼도
(09-07 19:00 → 09-08 02:57) 갱신토큰 만료는 09-11 04:25 그대로였다.
갱신토큰은 로그인 시점부터 약 28~29일 고정이고, 유일한 연장 수단은 브라우저 재인증이다.

그래서 **재로그인 절차를 대신 띄워주는 것**까지 자동화했다.

| 파일 | 역할 |
|---|---|
| `~/.claude/remote-control/token-keepalive.sh` | 감시·알림·창 기동 |
| `~/.claude/remote-control/relogin.command` | 재로그인 창 (수동 실행도 가능) |
| `~/Library/LaunchAgents/com.sanghyeon.claude-token-keepalive.plist` | 로그인 시 1회 + 6시간마다 |
| `~/.claude/remote-control/state/token.json` | 마지막 판정 (`last_alert`·`last_launch` 로 중복 억제) |
| `~/.claude/remote-control/logs/token-keepalive.log` | 기록 |

동작:
1. `claude auth status --json` 으로 로그인 상태 확인 (API 호출 없음)
2. 키체인 `Claude Code-credentials` 에서 **만료시각만** 읽는다 (토큰 값은 읽지 않는다)
3. **5일** 이하 → 알림 (12시간에 한 번)
4. **3일** 이하 → `relogin.command` 를 자동으로 띄운다 (24시간에 한 번, 이미 진행 중이면 안 띄움)
5. **2일** 이하 → 실행할 때마다 알림
6. 이미 로그아웃 상태면 즉시 창을 띄운다

### 브라우저 세션이 살아 있으면 사람 개입 없이 끝난다 (2026-09-08 실측)

창 기동을 시험하다가 **로그인이 그대로 완료됐다.** 브라우저에 Claude 로그인
세션이 남아 있으면 승인 화면이 자동 통과돼, 만료가 29일 뒤로 밀렸다
(2026-10-06 09:01 → 2026-10-07 19:54). 즉 브라우저만 로그인돼 있으면 실질적으로
완전 자동이고, 세션이 끊겨 있을 때만 사람이 승인 버튼을 누르면 된다.

### 창을 AppleScript 가 아니라 `open` 으로 띄우는 이유

`osascript` 로 Terminal 을 조종하려면 자동화(TCC) 권한이 필요한데, launchd 에서
도는 프로세스는 그 권한을 못 받아 **조용히 실패**할 수 있다. `open <파일>.command`
는 LaunchServices 경로라 권한 없이 된다. 그래서 `.command` 파일 방식을 썼다.

### 왜 이게 중요한가

데스크톱 앱 로그인과 CLI 로그인은 별개다. CLI 만 만료되면 launchd 세션 7개가
전부 안 뜨는데 앱은 멀쩡해서 눈치채기 어렵다. 윈도우 PC 가 2026-08-26 부터
그 상태였고 9/7 에야 발견됐다 — 12일을 몰랐다.

### 장기 토큰(`claude setup-token`)을 쓰지 않는 이유

월 1회 재인증을 없앨 수 있지만, 29일 토큰은 최악의 경우에도 한 달이면 저절로
소멸하는 반면 장기 토큰은 그 안전망이 없다. 유출 시 피해가 훨씬 크다.
자격증명을 새로 만드는 일은 사용자 판단 사항이므로 임의로 진행하지 않는다.

수동 재로그인:
```bash
open ~/.claude/remote-control/relogin.command
```

점검:
```bash
cat ~/.claude/remote-control/state/token.json
tail ~/.claude/remote-control/logs/token-keepalive.log
launchctl list | grep token-keepalive
```

중지: `launchctl bootout gui/$(id -u)/com.sanghyeon.claude-token-keepalive`

## 3. 키보드 — Karabiner-Elements

1. Karabiner-Elements 설치 (brew 또는 공식 사이트) + 권한 승인
2. [`setup/karabiner.json`](setup/karabiner.json) → `~/.config/karabiner/karabiner.json` 로 복사 (Karabiner가 자동 리로드)

들어있는 것: Ctrl+C/V/X/Z/A/S/F/T/W 등 → Cmd 변환, 오른쪽 Alt = 한/영(Ctrl+Opt+Space 전송), Home/End, Alt+F4, Ctrl+클릭 → Cmd+클릭(새 탭), **Ctrl+`+`/`-`/`0` → 브라우저 페이지 줌**, Ctrl+Esc = Launchpad 등.

### 함정 3가지 (전부 겪고 해결한 것)

- **마우스는 연결 방식마다 장치 ID가 다르다.** MX Master 3S 기준 블루투스 직결(product_id 45108)과 Logi Bolt 수신기(50504)를 **둘 다** devices에 등록해야 어느 쪽으로 연결해도 Ctrl+클릭이 먹는다. 새 마우스면 Karabiner EventViewer에서 ID 확인 후 추가
- **Caps Lock이 켜지면 규칙이 안 먹는다.** from에 `"optional": ["caps_lock"]`이 없는 규칙은 Caps Lock ON 상태에서 매칭 실패. setup의 json은 전 규칙에 반영돼 있음. **새 규칙 추가할 때도 필수**
- 맥북 **내장 트랙패드 클릭은 Karabiner가 못 잡는다** (Apple 장치 제한). Ctrl+클릭 새 탭은 외장 마우스에서만

## 4. Caps Lock = 대소문자 (한/영 전환 끄기)

```bash
defaults write com.apple.HIToolbox TISRomanSwitchState -int 0
killall TextInputMenuAgent
```

- 안 먹으면: 시스템 설정 → 키보드 → 입력 소스 **편집** → "Caps Lock 키로 ABC 입력 소스 전환" 끄기 (또는 재로그인)
- ⚠️ 시스템 설정 앱이 열려 있으면 defaults 쓰기가 덮어써질 수 있다 — 닫고 실행
- 한/영 전환은 오른쪽 Alt (3장 Karabiner)

## 5. 크롬 — Ctrl+휠 콘텐츠(페이지) 확대

맥 크롬은 Ctrl+휠 줌을 기본 지원하지 않고, 휠 이벤트는 Karabiner로도 못 바꾼다. **확장 프로그램으로만 가능.** 두 가지 중 하나:

- **웹스토어 "Quick zoom for MacOS"** (현재 사용 중): 설치 후 툴바 아이콘에서 보조 키를 Ctrl로
- **자작 확장** [`setup/ctrl-wheel-zoom/`](setup/ctrl-wheel-zoom/): `chrome://extensions` → 개발자 모드 → "압축해제된 확장 프로그램 로드" → 이 폴더 선택 (로드 후 폴더 이동 금지. 현 맥에서는 `~/Tools/ctrl-wheel-zoom`)

⚠️ 공통 함정:
- 확장 설치/활성화 **이전에 열려 있던 탭은 새로고침**해야 적용
- **로컬 HTML(file://) 페이지는 기본 차단** — 확장 세부정보에서 "파일 URL에 대한 액세스 허용" 켜야 동작
- 크롬 프로필 앱(Chrome-개인/개발/일반)마다 **각각** 설치해야 한다. 둘 다 설치하면 줌이 이중 적용되니 프로필당 하나만
- 프로필 앱 생성 스크립트는 `~/Tools/chrome-profile-launcher/` (만들어진 앱은 자체 완결형)

## 6. 폴더 구조 규칙 (2026-08-13 정리)

| 위치 | 용도 | 현재 내용 |
|---|---|---|
| `~/workspace/` | **git 프로젝트만** | AskMeAnything, Tokbell, tokbell_sender, morning_economy, rabbit-typing-adventure, kafka_test, webs.madang.ai, household_ledger, myCalendar |
| `~/Tools/` | 도구·유틸 | chrome-profile-launcher, ctrl-wheel-zoom, xtts-build(레거시), cosyvoice-build(레거시, 4.5GB 모델 포함) |
| `~/Archives/` | 보관물 | cw (코웨이 소스 백업 6.9GB) |

- xtts/cosyvoice는 rabbit 음성의 **레거시 도구** (2026-07-31에 파이프라인이 edge-tts로 교체됨). 옛 목소리 되살릴 때만 필요
- ⚠️ **`~/Tools/xtts-build/refs/sora_ref.wav`(1.2MB)는 어디에도 없는 유일본** — 옛 목소리(소라)의 레퍼런스. 이것만은 지우지 말 것. cosyvoice의 `pretrained_models/` 4.5GB는 지워도 재다운로드 가능
- cosyvoice-build의 `.venv`는 경로 하드코딩 때문에 이동 시 깨진다 → 새 맥에서는 재생성이 정답 (`.venv` 통째로 빼고 복사)

## 7. 새 맥 셋업 체크리스트 (요약)

```text
[ ] 1. Claude CLI 설치 + 로그인 (1장)
[ ] 2. 이 저장소 clone + 각 프로젝트 폴더 clone
[ ] 3. 각 폴더 trust 승인 + Remote Control 1회 동의 (2장)
[ ] 4. run.sh 폴더 목록 수정 → launchd 등록 (2장)
[ ] 5. 모바일 앱 목록에서 세션들 뜨는지 확인
[ ] 6. Karabiner 설치 + karabiner.json 복사 + 마우스 장치 ID 확인 (3장)
[ ] 7. Caps Lock 설정 (4장)
[ ] 8. 크롬 프로필 앱 생성 + 줌 확장 설치 (5장)
[ ] 9. ~/Tools, ~/Archives 필요분 복사 — sora_ref.wav 잊지 말 것 (6장)
```

---

## 7-1. 잠자기 깨어남 사고와 이어받기 체인 개정 (2026-08-14 오전)

**사고**: 밤새 잠자기 후 아침에 깨어나며 8개 세션이 전부 정상 종료 → 재시작 루프가 **새 빈 세션들을 만들어** 목록에 두 세대가 생기고 어제 대화가 "연결 끊김"으로 남았다.

**원인 3가지 (전부 실측)**:
1. claude는 **정상 종료 시 bridge-pointer.json(세션 기록)을 지운다.** 어제는 강제 종료(kickstart)라 파일이 살아남아 폴백이 동작했지만, 잠자기 후 곱게 죽자 기록이 사라졌다
2. `remote-control -c`를 `--spawn`과 함께 쓰면 **원래부터 에러다** — 어제도 -c는 늘 실패했고 폴백 2단계가 조용히 다 처리하고 있었다
3. 폴백 최종 단계가 "새 대화 생성"이라, 1·2가 무너지자 빈 세션을 만들어버렸다

**개정 (run.sh 2026-08-14)**:
- 실행 중 60초마다 bridge-pointer를 `state/`에 **스냅샷** → 기록이 지워지면 스냅샷으로 복원
- 체인 재설계: ① 기록/스냅샷의 세션 ID로 재접속(단독 `--session-id`) ② 없으면 단독 `-c` ③ **새 세션은 절대 만들지 않고 60초 후 재시도** (대화 보존 최우선. 새 폴더 등록은 옵션 `""`로 명시)
- 사고로 생긴 새 빈 세션들은 로그의 어제 세션 ID 7개로 bridge-pointer를 복원해 **전부 원래 대화로 복귀시켰다** (빈 세션들은 목록에서 보관 처리하면 됨)

## 8. 윈도우 → 맥 이관 실행 결과 (2026-08-13 저녁)

윈도우 측 [이관 인수인계서](2026-08-13_맥북-이관-인수인계.md)와 이관 꾸러미(zip)를 받아 실행 완료했다.

- **대화 기록 48개(110MB) + memory 전부 이관 완료.** 윈도우 인수인계서 §12의 미검증 항목들을 실측으로 해소:
  - 맥 대화 디렉터리 이름 규칙 = 경로의 비영숫자(`/`, `_`, `.`)를 전부 `-`로 치환 (예: `webs.madang.ai` → `-Users-sanghyeon-workspace-webs-madang-ai`)
  - **`.jsonl`은 OS 간 호환된다** — 윈도우 대화를 맥에서 `--resume`으로 열어 내용 요약까지 확인함
- memory의 `D:\...` 경로는 맥 경로로 일괄 수정. 단 `D:\연구소\...`는 이관 대상이 아닌 윈도우 전용 폴더라 그대로 두고 주석만 추가 (tokbell_sender·tokbell_ha)
- 미추적 산출물 복원: Tokbell 139개(절차서·소개서·스크립트), tokbell_sender 117개, kafka_test 9개(AWS 문서), webs 3개, morning_economy 1개
  - Tokbell 수정본 2개는 윈도우 스냅샷 base가 한 커밋 오래된 상태였음 → 전체 덮어쓰기 대신 **3-way 병합**으로 적용해 8/11 커밋(MFA·발송차단)을 보존함
  - 줄바꿈: 윈도우 CRLF 파일을 LF로 정규화 후 적용
- `tokbell_ha`(git 없음, 유일본) → `~/workspace/tokbell_ha` 복사 완료
- origin 정리: 별칭 없이 clone돼 **aiusertwo**(폐기 계정) 키로 붙던 4개 저장소(AskMeAnything, rabbit, kafka_test, morning_economy)를 올바른 별칭으로 교정
- SSH: 맥 자체 별칭(`github-sanghyeon83`, `github-ibank-ax`)이 정식. 윈도우 문서 호환용으로 `sanghyeon-github`, `github.com-ibank` 별칭도 같은 키로 추가해둠
- git 전역 설정 적용 (`autocrlf=input`, `quotepath=false` 등)
- Tokbell 브랜치는 dev가 아니라 **main 유지** — 맥에서 이미 main 위에 진행 중인 작업(SETUP-MAC.md 등)이 있었음. dev==main 동기화 상태였으므로 무방
- 오래된(수 시간·10만 토큰↑) 세션은 재시작 시 **요약으로 재개**될 수 있다(CLI 기본값) — 대화 계보는 유지되므로 문제없음

### Cursor 자체 채팅 — 앱 주입 성공, 단 추가 수리 2건 필요했음 (3차 꾸러미, 2026-08-13 밤)

윈도우의 `cursor-import/` 주입 스크립트는 그대로는 **목록 표시/본문 로딩이 안 됐다.** 맥에서 원인을 역추적해 두 가지를 고쳐 완성했다 (향후 같은 작업 시 필수):

1. **workspaceId 컬럼만 바꾸면 앱이 되돌린다.** 대화 소속은 헤더 value JSON 안의 `workspaceIdentifier`(id+uri)가 진짜 기준이다. 앱이 재저장하면서 컬럼을 JSON 기준으로 재계산해 윈도우 소속으로 회귀했다 → 1,590행의 JSON 내부 식별자를 맥 워크스페이스 것으로 교체(네이티브 행에서 템플릿 복사)해 해결
2. **값을 BLOB으로 넣으면 앱이 못 읽는다.** 본문 조회 함수가 `typeof value != "string"`이면 조용히 undefined를 반환해 "Loading Chat" 무한 대기가 된다. 파이썬 bytes 삽입 → BLOB이 원인 → 4개 도메인 230,868행을 `CAST(value AS TEXT)`로 변환해 해결
   - 진단 방법: `/Applications/Cursor.app/Contents/MacOS/Cursor --enable-logging=stderr`로 렌더러 콘솔을 파일로 받아 "[composer] No composer data handle found" 확인
3. **이미지도 이식 완료 (cursor-images.zip, 2026-08-13 밤).** 이미지는 두 방식으로 저장돼 있었다:
   - 버블 JSON에 인라인(58개) → 1차 이식 때 이미 포함
   - `workspaceStorage/<wsid>/images/*.png` 별도 파일(1,918개) → 추가 꾸러미로 수령, **A방식**으로 이식:
     ① 파일을 맥 `workspaceStorage/<맥 wsid>/images/`로 프로젝트별 복사
     ② 버블 속 윈도우 절대경로(`c:\Users\...\workspaceStorage\<윈 wsid>\images\...`)를 맥 경로로 치환 (1,346행, wsid 재매핑 포함)
   - 매칭은 manifest.json(uuid)이 아니라 **path-references.json**(버블 본문의 경로에서 추출) 기준 — 윈도우 세션 안내대로
   - 윈도우 원본에도 없는 임시 이미지 1,591개 참조는 복구 불가(확인됨). 잔존 윈도우 경로 108행은 이미지가 아닌 터미널 참조 등이라 무해
   - ✅ 사용자 육안 확인 완료: 목록·본문·이미지 모두 정상

#### 원래 주입 절차 기록

윈도우 세션이 만든 `cursor-import/`(이식용 SQLite 2.48GB + 주입 스크립트)로 **윈도우 Cursor 대화 1,590개를 맥 Cursor 앱에 실제 주입 완료.**

- 사전 점검: 맥/윈도우 `composerHeaders` 스키마 동일 확인, 워크스페이스 매핑 8/8 (tokbell_ha는 Cursor로 폴더를 한 번 열어 생성)
- **함정 발견·해결**: `~/morning_economy`(잘못된 경로)로 열었던 스테일 워크스페이스가 있어 매핑이 비결정적이었음 → `~/Archives/cursor-workspaceStorage-stale/`로 이동 후 진행
- 주입 결과: 대화 1,590 + 본문 230,854행, 총 1,676개(맥 기존 84 + 주입분), 무결성 ok, 15,266버블 대화 연결 확인
- 되돌리기: `state.vscdb.bak-20260813-193207` (globalStorage에 자동 백업됨)
- 도구 위치: `~/Tools/cursor-import/` (주입 완료 후에는 보관용)

### Cursor 자체 채팅 읽기용 아카이브 (2차 꾸러미, 2026-08-13 저녁)

- 윈도우 Cursor의 Chat/Composer 대화 **1,563개(38,495 메시지)** 추출본을 받아 **`~/Archives/cursor-chats/`**에 배치
  - `_index.md`(전체 목록) → 프로젝트별 `.md`(읽기용, 사고과정·도구호출 포함) / `_json/`(무손실 원본)
  - ⚠️ **Cursor에는 import 기능이 없어 읽기 전용 아카이브다.** 맥 Cursor의 Chat 탭에는 안 뜬다. 과거 Cursor 대화를 찾을 일이 있으면 이 아카이브를 검색할 것
  - Claude Code 대화(48개)와는 별개다 — 그쪽은 이관 완료돼 CLI·Cursor의 Claude Code 패널에서 그대로 이어짐

### 이관에서 남은 것 (사용자 직접)

- [ ] **윈도우 시작프로그램 `.vbs` 비활성화** — 안 하면 윈도우 부팅 때마다 세션 8개가 계속 생성됨
- [ ] `D:\workspace\Tokbell\.env bk` — 비밀정보라 꾸러미에 없음. 필요하면 직접 이관
- [ ] `~/.claude/settings.json` — 윈도우 값(꾸러미 `config/claude-settings.json`)을 적용할지 결정 (권한 설정이라 자동 적용하지 않음)
- [x] ~~기본 SSH 키가 폐기 계정 aiusertwo에 연결~~ → **해결(2026-08-13)**: `Host github.com` 블록을 추가해 별칭 없는 접속은 `id_ed25519_ibank-ax`(ibanksanghyeon)로 인증되게 함
