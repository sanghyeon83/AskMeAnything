# PC 작업 지시 — Remote Control 마무리 (2026-08-10, 클라우드 세션 인수인계)

## 배경 (완료된 것)

- 08-10 재부팅에서 8개 세션의 `-c`가 전부 실패 → 빈 대화 폴백 발동
- 클라우드 쪽에서 처리 완료:
  - 빈 1세대 세션 7개 전부 아카이브 (Tokbell HA / Tokbell / Tokbell Sender / Rabbit Typing / Webs Madang / Morning Economy / AskMeAnything)
  - 대화가 이어져 있는 **2세대 세션(태그 `remote-control-repl`, 앱에서 "기타" 그룹)이 진짜**임을 확인
  - 운영 문서 전면 재작성 → `claude/askmenanything-cloud-jr8bzk` 브랜치에 푸시됨
- 남은 문제 두 가지가 이 문서의 할 일이다:
  1. 2세대 세션들이 폴더 메타데이터가 없어 앱 목록에서 "기타"로 밀려 있음
  2. `.vbs` 시작 대기가 20초라 다음 재부팅에도 같은 사고가 날 수 있음

## 할 일

### 1. `.vbs` 시작 대기 시간 20초 → 90초

- 파일: `C:\Users\sanghyeon\AppData\Roaming\Microsoft\Windows\Start Menu\Programs\Startup\claude-remote-control.vbs`
- 부팅 직후 네트워크가 안 올라온 상태에서 `-c`가 클라우드 조회에 실패하는 게 8개 동시 폴백의 유력 원인 → 대기를 90초로 늘린다
- ⚠️ 저장은 **UTF-16LE(BOM FF FE)** 유지
- ⚠️ 수정 후 `RunRC` 줄 개수 확인 — **8개**여야 한다 (kafka 줄 누락 여부 특히 확인)

### 2. 세션을 정상 방식으로 재기동 → 목록 그룹 복구

지금 떠 있는 2세대는 복구용 repl(SDK) 방식이라 폴더/브랜치 메타데이터를 서버에 안 올려서 "기타"로 분류된다. `.vbs` 정상 경로로 다시 띄우면 메타데이터가 올라가 제자리 그룹으로 돌아간다.

순서:

1. 현재 remote-control 프로세스 전부 종료
2. `wscript.exe "<.vbs 경로>"` 실행 (1번의 90초 수정이 반영된 상태로)
3. 각 폴더의 `-c`가 **가장 최근 대화 = 2세대 세션**을 이어받는지 확인

⚠️ **핵심 검증**: 재기동 후 태블릿에서 세션마다 "우리가 지금까지 뭐 하고 있었지?"라고 물어서 이전 대화를 기억하는지 확인한다. **세션이 떴는지가 아니라 대화를 물고 왔는지가 기준이다.**

⚠️ 만약 `-c`가 2세대를 못 물고 또 빈 대화가 뜨면, 즉시 중단하고 `.vbs` 세 번째 인자를 `--session-id`로 고정한다. 2세대(대화 살아있는 쪽) ID:

| 폴더 | 세션 ID |
|---|---|
| kafka_test | `session_01AJJMcbYzLnAC9iTnCQ15d3` |
| morning_economy | `session_01M9EGJzPsLNn14r22TCg68b` |
| rabbit-typing-adventure | `session_017kMnmHKXLWXT83niCuK5xj` |
| tokbell_sender | `session_011i7YWxaLuiXRiyRYS9tMPY` |
| Tokbell | `session_019pT9xxkhHYbXhpKxxK2pQb` |
| tokbell_ha | `session_01RZNAEmUXtr4DJ5fd1hdazk` |
| webs.madang.ai | `session_014gYcCMWsm6y6V4Z118DCZg` |

- AskMeAnything은 2세대가 없다 (복구 세션은 사용자 지시로 아카이브함). 빈 대화로 새로 시작하는 게 정상이다

### 3. 문서 받아오기

`D:\test_workspace\AskMeAnything`에서:

```powershell
git fetch origin claude/askmenanything-cloud-jr8bzk
git checkout claude/askmenanything-cloud-jr8bzk
git pull origin claude/askmenanything-cloud-jr8bzk
```

- `2026-07-27_remote-control_정리.md`가 운영 가이드로 전면 재작성돼 있다. 앞으로 이 문서 기준으로 운영
- 이 인수인계 파일(`PC_인수인계_2026-08-10.md`)은 작업 완료 후 삭제해도 된다

## 최종 검증 체크리스트

- [ ] `.vbs` 대기 90초, UTF-16LE, `RunRC` 8줄
- [ ] 실행 중 claude 프로세스 개수 = 8
- [ ] 태블릿 목록에서 세션들이 "기타"가 아닌 각 프로젝트 그룹에 표시
- [ ] 세션마다 이전 대화 기억 확인 (AskMeAnything만 빈 대화가 정상)
- [ ] 확인 끝나면 이 파일 삭제
