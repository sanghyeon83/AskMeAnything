# Windows → 맥북 이관 인수인계서

> 작성: 2026-08-13 / 작성 주체: Windows PC(`DESKTOP-PARKSH`)의 Claude Code 세션
> **읽는 사람: 맥북에서 이 작업을 이어받을 에이전트**
> 이 문서의 수치·경로는 2026-08-13 기준 실측값이다. 추측한 부분은 "⚠️ 미검증"으로 표시했다.

---

## 0. 한눈에 보기

사용자는 Windows PC에서 하던 **9개 프로젝트 전부를 맥북으로 옮기고, 지난 대화 기록까지 그대로 가져가려 한다.**
맥북에는 Cursor가 설치돼 있다.

옮겨야 할 것은 네 덩어리다.

| 덩어리 | 위치(Windows) | 크기 | 이관 방법 |
|---|---|---|---|
| ① 프로젝트 소스 | `D:\workspace\`, `D:\test_workspace\` | 약 7GB | 대부분 git clone, 2개는 수동 복사 |
| ② 대화 기록 | `%USERPROFILE%\.claude\projects\` | **110.9MB / 140파일** | 디렉터리명 변환 후 복사 (§4 — 가장 까다로움) |
| ③ 기억(memory) | 위 디렉터리 안의 `memory\` | 6개 프로젝트분 | ②와 함께 이동 |
| ④ 환경 설정 | `~/.ssh`, git 전역, `~/.claude` | 소량 | §5, §6 |

---

## 1. 프로젝트 목록 (실측)

| 프로젝트 | Windows 경로 | 크기 | git 원격 | 브랜치 | 미커밋 |
|---|---|---|---|---|---|
| Tokbell | `D:\workspace\Tokbell` | 3,334MB | `git@github.com-ibank:ibank-ax/Tokbell.git` | dev (main 머지완료) | 139 (전부 미추적) |
| tokbell_sender | `D:\workspace\tokbell_sender` | 1,521MB | `git@github.com-ibank:ibank-ax/tokbell_sender.git` | **main** (머지완료) | 118 (전부 미추적) |
| rabbit-typing-adventure | `D:\workspace\rabbit-typing-adventure` | 1,042MB | `git@github.com-ibank:sanghyeon83/rabbit-typing-adventure.git` | main | 0 |
| webs.madang.ai | `D:\workspace\webs.madang.ai` | 642MB | `git@github.com-ibank:ibank-ax/webs.madang.ai.git` | main | 3 |
| morning_economy | `D:\workspace\morning_economy` | 336MB | `git@sanghyeon-github:sanghyeon83/morning_economy.git` | main | **664** |
| kafka_test | `D:\workspace\kafka_test` | 89MB | `git@github.com-ibank:ibank-ax/kafka_test.git` | dev | 10 |
| AskMeAnything | `D:\test_workspace\AskMeAnything` | 0.2MB | `git@sanghyeon-github:sanghyeon83/AskMeAnything.git` | master | 0 |
| **tokbell_ha** | `D:\test_workspace\tokbell_ha` | 0.7MB | **git 아님** | — | — |
| test | `D:\test_workspace\test` | 0MB | **git 아님** | — | — |

### 커밋·머지 상태 (2026-08-13 재확인)

**Tokbell·tokbell_sender는 사용자가 main 머지를 완료했고, origin과 완전히 동기화돼 있다** (ahead/behind 0).
따라서 **커밋된 작업은 clone으로 전부 따라온다.**

```
Tokbell         dev  -> origin/dev  [동기화됨]   main -> origin/main [동기화됨]
tokbell_sender  main -> origin/main [동기화됨]   dev  -> origin/dev  [동기화됨]
```

### 🔴 남은 위험 — git이 추적하지 않는 파일 (머지와 무관)

머지 후에도 **미추적(`??`) 파일이 Tokbell 137개(26.2MB) / tokbell_sender 118개(0.7MB)** 남아 있다.
git에 없으므로 **clone으로는 절대 따라오지 않는다.** 확인해 보니 버릴 것이 아니라 실제 업무 산출물이었다.

| 프로젝트 | 미추적 내용 |
|---|---|
| Tokbell | 서버 구축 절차서 `.docx` 4종(DB / Jenkins / Redis·RabbitMQ / 쿠버네티스), 서비스 소개서 PDF 3종(v1.0/1.1/1.2, 25MB), Postman 컬렉션, `apps/api/scripts/` 운영 스크립트 다수(backfill·analyze·inspect 등), `.cursor` 설정 |
| tokbell_sender | `scripts/` 정산·감사 리포트 JSON(audit-credit, clawback-overrefund, moonjoohye-settlement 등)과 `tmp-*.mjs` 분석 스크립트 |

**→ 이관 꾸러미의 `untracked/` 에 전부 담아두었다.** clone 후 그대로 덮어쓰면 된다.

또한 Tokbell에는 **추적 중인데 수정된 파일 2개**가 있다 (clone하면 커밋본이 와서 수정분이 사라진다).

```
apps/web/src/app/messages/kakao/alimtalk/page.tsx
apps/web/src/app/templates/alimtalk/new/page.tsx
```

꾸러미의 `untracked/Tokbell/_modified_files/` 에 실물을, `_modified.diff` 에 변경분을 함께 넣었다.
(diff는 `git apply --check -R` 로 유효성 검증 완료.)

### 🔐 꾸러미에서 제외한 것 — 직접 옮길 것

- `D:\workspace\Tokbell\.env bk` — 환경변수 백업. 비밀정보라 넣지 않았다. **필요하면 사용자가 직접 옮길 것**
- `; done`, `; done' ssh -o ... root@49.247.43.227 ...` — 셸 명령이 파일명으로 잘못 만들어진 쓰레기. 버려도 된다

### 그 밖의 미커밋

- **morning_economy 664건** — 전부 `data/blog_posts/`의 생성 산출물(`.json` 332, `.html` 331).
  소스가 아니므로 **버려도 되는지 사용자에게 확인**할 것. 발행 이력으로 보관 중일 수 있다. (꾸러미에 미포함)
- kafka_test 10건, webs.madang.ai 3건 — 소량, 확인 후 처리 (꾸러미에 미포함)

### ⚠️ git worktree 2개 — 별도 존재

두 저장소가 **워크트리를 쓰고 있다.** clone만 하면 이 브랜치들의 작업 상태가 재현되지 않는다.

| 저장소 | 워크트리 경로 | 브랜치 | 맥에서 |
|---|---|---|---|
| Tokbell | `D:/workspace/tokbell-mainsync` | `main` | 필요하면 `git worktree add` 로 재생성 (origin/main에 있음) |
| tokbell_sender | `C:/Users/sanghyeon/.cursor/worktrees/graceful-drain-f08a530a` | `feature/graceful-drain-sending-idempotency` | **불필요** — 안 쓰는 `aiuserone` 원격 추적 브랜치 |

`tokbell-mainsync` 는 main 동기화 전용 워크트리라 clone 후 `main` 브랜치를 직접 쓰면 대체된다.
`graceful-drain...` 은 Cursor가 만든 워크트리다.

### 원격은 origin 하나만 옮긴다 (사용자 확인 완료)

Windows 저장소에는 `origin` 외에 개인 계정 원격(`aiusertwo` / `aiuserone`, `github.com-ai` 별칭)이
붙어 있으나 **사용자가 더 이상 쓰지 않는다고 확인했다 (2026-08-13).**

**맥에서는 `origin`만 있으면 된다. `git remote add` 로 추가하지 말 것.**

```
Tokbell         origin = git@github.com-ibank:ibank-ax/Tokbell.git
tokbell_sender  origin = git@github.com-ibank:ibank-ax/tokbell_sender.git
```

따라오는 결과:

- `~/.ssh` 에서 **`ai_github` 키는 옮길 필요 없다** (§5는 `id_ed25519_ibank`, `sanghyeon_github` 두 개만 필요)
- 그 원격을 추적하던 아래 브랜치들도 **재현할 필요 없다**
  - Tokbell `chore/prod-hotfix-mirror` (`aiusertwo/main` 기준 77커밋 뒤처짐)
  - tokbell_sender `feature/graceful-drain-sending-idempotency` (Cursor 워크트리용)

### 🔴 git이 아닌 폴더 2개 — 반드시 수동 복사

`D:\test_workspace\tokbell_ha` 는 **원격 저장소가 없다.** 복사하지 않으면 영구 소실된다. 내용물:

```
ha\00-임원보고-이중화.html            41.4KB      ha\03-이중화-예산산출.html         44.0KB
ha\00-작업요약-전체.md                 9.3KB      ha\03-이중화-예산산출.md           14.0KB
ha\01-현황진단-이중화.md              11.7KB      ha\04-Cursor작업지시-2단계-DB이중화.md  8.5KB
ha\01_현황진단.md                     9.1KB      ha\dashboard.html                27.2KB
ha\02-점검증적-부하분산·이중화.md        20.0KB      ha\manifests\*.yaml               2개
ha\_extract\  (doc1~4.txt, *.py, report.png 308KB)  ha\.claude\settings.local.json
```

임원보고서·예산산출 등 **업무 산출물**이다. `D:\test_workspace\test`는 비어 있어 무시해도 된다.

---

## 2. 맥북 경로 — 확정됨 ✅

사용자 확인 완료 (2026-08-13). **맥에서는 `test_workspace`를 따로 두지 않고 `~/workspace` 하나로 통합한다.**

```
/Users/sanghyeon/workspace/Tokbell
/Users/sanghyeon/workspace/tokbell_sender
/Users/sanghyeon/workspace/rabbit-typing-adventure
/Users/sanghyeon/workspace/webs.madang.ai
/Users/sanghyeon/workspace/kafka_test
/Users/sanghyeon/workspace/morning_economy
/Users/sanghyeon/workspace/AskMeAnything      ← Windows에서는 test_workspace 밑이었음
/Users/sanghyeon/workspace/tokbell_ha         ← Windows에서는 test_workspace 밑이었음
```

`~/workspace`는 이미 존재한다. `D:\test_workspace\test`는 빈 폴더라 옮기지 않는다.

| Windows | 맥 |
|---|---|
| `D:\workspace\<프로젝트>` | `~/workspace/<프로젝트>` |
| `D:\test_workspace\AskMeAnything` | `~/workspace/AskMeAnything` |
| `D:\test_workspace\tokbell_ha` | `~/workspace/tokbell_ha` |
| `D:\test_workspace\test` | (이관 안 함) |

> ⚠️ AskMeAnything과 tokbell_ha는 **부모 폴더가 바뀐다.** 두 프로젝트의 문서·memory에 적힌
> `D:\test_workspace\...` 경로는 `~/workspace/...` 로 고쳐야 한다 (§4 마지막 참고).

---

## 3. 프로젝트 이관 절차

```bash
cd ~/workspace
git clone git@github.com-ibank:ibank-ax/Tokbell.git
git clone git@github.com-ibank:ibank-ax/tokbell_sender.git
git clone git@github.com-ibank:sanghyeon83/rabbit-typing-adventure.git
git clone git@github.com-ibank:ibank-ax/webs.madang.ai.git
git clone git@github.com-ibank:ibank-ax/kafka_test.git
git clone git@sanghyeon-github:sanghyeon83/morning_economy.git
git clone git@sanghyeon-github:sanghyeon83/AskMeAnything.git
# tokbell_ha 는 git이 아님 → 이관 꾸러미의 projects/tokbell_ha 를 ~/workspace/ 로 복사
```

clone 후 브랜치 맞추기 (Tokbell·tokbell_sender·kafka_test는 `dev`):

```bash
cd ~/workspace/Tokbell && git checkout dev
cd ~/workspace/tokbell_sender && git checkout dev
cd ~/workspace/kafka_test && git checkout dev
```

> ⚠️ **clone 주소의 `github.com-ibank` / `sanghyeon-github` 는 실제 호스트가 아니라 SSH 별칭이다.**
> §5를 먼저 해두지 않으면 clone이 전부 실패한다.

---

## 4. 대화 기록 이관 (가장 까다로운 부분)

### 원리

Claude Code는 대화를 프로젝트 폴더 경로에서 만든 디렉터리 이름 아래 보관한다.

```
Windows: C:\Users\sanghyeon\.claude\projects\D--test-workspace-AskMeAnything\*.jsonl
맥:      ~/.claude/projects/<맥 경로로 만든 이름>/*.jsonl
```

Windows에서 관측된 변환 규칙: **`:` 와 경로 구분자 `\` → `-`, 그리고 `.` `_` → `-`**
예) `D:\workspace\webs.madang.ai` → `D--workspace-webs-madang-ai`
   `D:\test_workspace\tokbell_ha` → `D--test-workspace-tokbell-ha`

맥에서는 `/Users/<사용자>/workspace/Tokbell` → `-Users-<사용자>-workspace-Tokbell` 형태가 될 것으로 보인다.

> ⚠️ **미검증 — 절대 추측으로 진행하지 말 것.**
> 맥에서의 정확한 이름은 아래 방법으로 **실제로 확인한 뒤** 복사해야 한다. 이름이 한 글자만 달라도 대화가 안 붙는다.

### 안전한 절차 (권장)

```bash
# 1) 대상 폴더에서 claude를 한 번 실행해 trust 승인 → /exit
#    (이 과정에서 맥이 스스로 올바른 이름의 디렉터리를 만든다)
cd ~/workspace/Tokbell && claude       # trust Yes → /exit

# 2) 방금 만들어진 디렉터리 이름을 확인
ls -1 ~/.claude/projects/

# 3) 그 디렉터리 안으로 Windows의 .jsonl 과 memory/ 를 복사
#    (디렉터리를 새로 만들지 말고, 2)에서 확인된 이름 안에 넣는다)
```

이 방식이면 규칙을 몰라도 틀릴 수가 없다. 프로젝트 9개에 대해 반복한다.

### 옮길 대상 (실측)

| Windows 디렉터리 | 대화 수 | 크기 | memory |
|---|---|---|---|
| `D--workspace-rabbit-typing-adventure` | 8 | **75.94MB** | 있음 |
| `D--test-workspace-tokbell-ha` | 10 | 19.37MB | 있음 |
| `D--workspace-tokbell-sender` | 7 | 5.02MB | 있음 |
| `D--test-workspace-AskMeAnything` | 9 | 3.99MB | 있음 |
| `D--workspace-morning-economy` | 7 | 2.35MB | 있음 |
| `D--workspace-kafka-test` | 2 | 1.46MB | 있음 |
| `D--test-workspace-test` | 3 | 0.75MB | — |
| `D--workspace-Tokbell` | 3 | 0.61MB | 있음 |
| `D--workspace-webs-madang-ai` | 2 | 0.45MB | — |
| `D--test-workspace-kafka` | 1 | 0.04MB | — (구 폴더 잔재, 생략 가능) |

**합계 140파일 / 110.9MB.** USB·클라우드 드라이브·`scp` 어느 방법이든 무방하다.

### memory 파일 (프로젝트별 장기 기억 — 반드시 함께 옮길 것)

각 프로젝트 디렉터리 안의 `memory/` 하위. `MEMORY.md`가 색인이고 나머지가 실제 기억이다.

- **tokbell-ha** (9개): `full-path-links.md`, `report-visual-standard.md`, `tokbell-backup-reality.md`,
  `tokbell-db-not-replicated.md`, `tokbell-db-storage-overprovisioned.md`, `tokbell-ingress-spof.md`,
  `tokbell-servers-overspecced.md`, `tokbell-work-split-cursor.md`, `MEMORY.md`
- **tokbell_sender** (7개): `cert-2026-checklist-doc.md`, `cert-2026-deadlines.md`, `cert-2026-dev-gaps.md`,
  `front-api-not-in-repo.md`, `handover-docs-location.md`, `infra-exposure-2026-08.md`, `memory.md`
- **rabbit-typing-adventure** (5개): `deploy-means-commit-push.md`, `fix-all-modes-together.md`,
  `reader-level.md`, `tablet-first.md`, `MEMORY.md`
- **AskMeAnything** (2개): `tablet-remote-control-setup.md`, `MEMORY.md`
- **morning_economy** (2개): `remote-control-setup.md`, `MEMORY.md`
- **kafka_test** (2개): `oa-aws-project-status.md`, `MEMORY.md`

> 🔴 memory 안에는 **Windows 절대경로가 박혀 있다** (예: `D:\test_workspace\AskMeAnything\...`).
> 이관 후 맥 경로로 일괄 수정해야 한다. 안 고치면 에이전트가 없는 파일을 찾는다.
> 특히 `full-path-links.md`, `handover-docs-location.md`는 이름부터 경로 관련이라 우선 확인할 것.

---

## 5. SSH 설정 (clone 전에 반드시)

Windows `~/.ssh/config` 원문:

```
Host github.com-ai
  HostName github.com
  User git
  IdentityFile ~/.ssh/ai_github
  IdentitiesOnly yes

Host sanghyeon-github
  HostName github.com
  User git
  IdentityFile ~/.ssh/sanghyeon_github
  IdentitiesOnly yes

Host coway-github
  HostName github.com
  User git
  IdentityFile ~/.ssh/coway_ed25519
  IdentitiesOnly yes

Host github.com-ibank
  HostName github.com
  User git
  IdentityFile ~/.ssh/id_ed25519_ibank
  IdentitiesOnly yes
```

이 프로젝트들이 실제로 쓰는 별칭은 **`github.com-ibank`** 와 **`sanghyeon-github`** 두 개다.
해당 키 파일 `id_ed25519_ibank`, `sanghyeon_github` (+ `.pub`)를 옮기면 된다.

```bash
chmod 700 ~/.ssh
chmod 600 ~/.ssh/id_ed25519_ibank ~/.ssh/sanghyeon_github ~/.ssh/config
ssh -T git@github.com-ibank        # 인증 확인
ssh -T git@sanghyeon-github
```

> 🔐 **개인키는 사용자가 직접 안전한 경로로 옮겨야 한다.** 에이전트가 키 내용을 읽거나 채팅·저장소에
> 올려서는 안 된다. 새 키를 만들어 GitHub에 등록하는 편이 더 안전하다면 그렇게 해도 된다.
> 맥에서는 `UseKeychain yes` / `AddKeysToAgent yes` 를 config에 추가하면 편하다.

---

## 6. git 전역 설정

Windows 현재값:

```
user.name  = sanghyeon83
user.email = pfreedomdev@gmail.com
core.autocrlf      = true                 ← 맥에서는 바꿔야 함
core.quotepath     = false                ← 한글 파일명 위해 유지
core.excludesfile  = C:\Users\sanghyeon\Documents\gitignore_global.txt  ← 경로 변경 필요
credential.helper  = wincred              ← 맥에서는 osxkeychain
i18n.commitencoding=utf-8 / i18n.logoutputencoding=utf-8 / gui.encoding=utf-8
```

맥에서:

```bash
git config --global user.name  sanghyeon83
git config --global user.email pfreedomdev@gmail.com
git config --global core.autocrlf input          # ⚠️ true 아님! 맥은 input
git config --global core.quotepath false         # 한글 파일명 깨짐 방지 (중요)
git config --global credential.helper osxkeychain
git config --global i18n.commitencoding utf-8
git config --global i18n.logoutputencoding utf-8
# gitignore_global.txt 도 옮기고 경로 재지정
```

> ⚠️ `core.autocrlf=true`를 그대로 가져가면 맥에서 줄바꿈이 어긋나 **파일 전체가 변경된 것처럼 보인다.**
> 맥은 반드시 `input`.

---

## 7. Claude Code 설정

### 전역 `~/.claude/settings.json` (Windows 현재값 — 그대로 써도 됨)

```json
{
  "permissions": {
    "allow": ["Bash","PowerShell","Read","Edit","Write","Glob","Grep",
              "WebFetch","WebSearch","Skill","Agent","Task","TaskCreate",
              "TaskUpdate","NotebookEdit"],
    "defaultMode": "dontAsk"
  },
  "autoUpdatesChannel": "latest",
  "skipDangerousModePermissionPrompt": true,
  "theme": "light-ansi",
  "agentPushNotifEnabled": true,
  "inputNeededNotifEnabled": true
}
```

> 맥에는 `PowerShell` 도구가 없다. 허용 목록에 남아 있어도 무해하지만 지워도 된다.

### 프로젝트별 `.claude/`

아래 프로젝트에 `settings.local.json` 등이 있다. git에 포함된 것도, 아닌 것도 있으니 clone 후 확인:

`Tokbell`, `tokbell_sender`, `rabbit-typing-adventure`(+`launch.json`), `webs.madang.ai`(`launch.json`),
`kafka_test`, `morning_economy`(`settings.json`+`settings.local.json`), `tokbell_ha`

### trust

`~/.claude.json`의 `hasTrustDialogAccepted` 값은 **옮겨도 소용없다.** 맥에서 각 폴더마다
`claude` 한 번 실행 → Yes → `/exit` 로 새로 승인해야 한다 (§4의 1단계와 동일 작업이므로 함께 처리).

---

## 8. Remote Control (태블릿 원격 조종) 재구성

사용자는 **태블릿(Claude 앱)에서 PC 세션을 원격 조종**해 왔다. 맥으로 옮기면 이 구성도 다시 만들어야 한다.

- Windows에서는 시작프로그램 `.vbs`가 부팅 시 프로젝트별 세션을 백그라운드 상주시켰다
- 상세 배경·함정은 같은 저장소의 **`2026-08-10_remote-control_정리.md`** 를 반드시 읽을 것
  (AskMeAnything 저장소에 있으므로 clone하면 바로 읽힌다)

### 맥에서의 대응

`.vbs` 대응물은 **launchd LaunchAgent**(`~/Library/LaunchAgents/*.plist`)다.
plist는 스크립트가 아니라 선언형 설정이라, 사용자의 "래퍼 스크립트 만들지 않음" 방침과 충돌하지 않는다.

각 프로젝트에서 실행할 명령 형태 (Windows와 동일):

```bash
claude --resume <로컬대화UUID> --remote-control '<표시이름>'
```

### 🔴 반드시 알아야 할 함정 (Windows에서 실제로 겪은 것들)

1. **`-c` / `--continue` 쓰지 말 것.** 로컬 대화 파일을 보지 않아 재부팅에 취약하다.
   2026-08-10에 이것 때문에 8개 세션 대화가 전부 끊겼다
2. **`--resume <UUID>` 는 대화를 새로 시작하면 낡는다.** 갱신을 잊으면 며칠 뒤 옛 대화로 깨어난다
   (2026-08-13 사고). 대화를 새로 시작했으면 **그 자리에서** 핀을 갱신할 것
3. **`.jsonl`의 mtime으로 "최신 대화"를 고르지 말 것 — 정반대를 집는다.**
   판별은 파일 안의 사용자 발화(`type: user`) 타임스탬프로 해야 한다.
   사용자 발화를 셀 때 `content`가 문자열인 것만 세면 안 된다 — 이미지를 붙인 메시지는 배열이라 누락된다
4. **처음 만드는 세션 이름은 QR 연결을 한 번 거쳐야** 태블릿 목록에 나타난다
5. **재부팅할 때마다 태블릿 목록에 항목이 새로 쌓인다.** 이건 구조적인 것이라 못 막는다
   (매 기동이 새 클라우드 세션을 만든다). 등록 세션 수를 줄이는 게 유일한 완화책

### 🔴 Windows 쪽 정리 — 잊지 말 것

맥으로 완전히 옮긴다면 **Windows의 시작프로그램 `.vbs`를 비활성화**해야 한다.
안 그러면 Windows를 켤 때마다 세션 9개가 계속 생겨 태블릿 목록을 어지럽힌다.
(사용자가 최근 "목록이 너무 늘어난다"고 한 원인 중 하나다.)

```
%APPDATA%\Microsoft\Windows\Start Menu\Programs\Startup\claude-remote-control.vbs
```

세션 이름은 맥/윈도우를 구분할 수 있게 정하는 게 좋다. 현재 태블릿 목록에 `_맥` 접미사가 붙은
항목들이 이미 있는데, 이 PC에서 만든 것이 아니다(호스트명 `DESKTOP-PARKSH`). 사용자에게 확인할 것.

---

## 9. 프로젝트별 인계 메모

- **morning_economy** — 로컬 Chrome·티스토리 발행·스케줄러가 필요한 작업이다. 클라우드 세션(Linux VM)에서는
  불가능해서 로컬 실행을 유지해 왔다. **맥에서도 Chrome과 스케줄러(launchd/cron) 재구성이 필요하다**
- **tokbell_ha** — 이중화(HA) 컨설팅 산출물. git 없음, 임원보고서·예산안 포함. memory 9개가 이 프로젝트의
  핵심 자산이다 (백업 실태, DB 미복제, Ingress SPOF, 서버 과다스펙 등 진단 결과)
- **tokbell_sender** — 2026 인증 관련 마감·미비점 메모가 memory에 있다. 날짜 있는 항목이라 우선 확인
- **rabbit-typing-adventure** — 대화 기록이 75.94MB로 가장 크다. 복사 시간 여유를 둘 것
- **AskMeAnything** — Remote Control 운영 문서 보관용 저장소. 이 문서와 `2026-08-10_remote-control_정리.md`가 여기 있다

---

## 10. 실행 체크리스트

```
□ 0. 맥북 폴더 구조 확정 (§2) — 대화 기록 이관이 여기 종속됨
□ 1. Windows 미커밋 정리 (§1) — Tokbell 139 / tokbell_sender 131 / morning_economy 664
□ 2. SSH 키·config 이관 + 권한 설정 + ssh -T 확인 (§5)
□ 3. git 전역 설정 (§6) — autocrlf=input 주의
□ 4. 프로젝트 clone + 브랜치 전환 (§3)
□ 5. tokbell_ha 수동 복사 (git 없음 — 누락 시 영구 소실)
□ 6. 각 폴더에서 claude 1회 실행 → trust 승인 → /exit (§7)
□ 7. ~/.claude/projects/<생성된 이름>/ 확인 후 .jsonl + memory/ 복사 (§4)
□ 8. memory 안의 Windows 절대경로를 맥 경로로 수정 (§4)
□ 9. 대화가 실제로 이어지는지 확인 — claude --resume 로 열어 내용 확인
□ 10. Remote Control 재구성 (§8) + Windows .vbs 비활성화
□ 11. morning_economy 스케줄러·Chrome 재구성 (§9)
```

---

## 11. 이관 후 검증

```bash
# 대화가 제대로 붙었는지 (프로젝트마다)
ls -1 ~/.claude/projects/*/ | head
claude --resume        # 목록에 과거 대화가 보이면 성공

# git 상태
for d in ~/workspace/*/ ~/test_workspace/*/; do
  [ -d "$d/.git" ] && echo "$d $(git -C "$d" rev-parse --abbrev-ref HEAD)"
done
```

**최종 확인은 각 프로젝트에서 `claude --resume`로 과거 대화를 열어 내용이 보이는지 직접 보는 것이다.**
파일 개수나 크기만으로 성공을 판단하지 말 것.

---

## 12. 이 문서를 쓴 세션이 확인하지 못한 것

정직하게 남긴다. 맥북 에이전트가 직접 확인해야 한다.

- 맥의 `~/.claude/projects/` **디렉터리 이름 생성 규칙** — Windows 규칙에서 유추만 했다 (§4의 절차대로 실측할 것)
- 대화 `.jsonl`을 **다른 OS로 옮겼을 때 그대로 열리는지** — 검증한 적 없다. 경로가 박힌 항목이 있을 수 있다
- 맥 launchd로 Remote Control 상주가 **Windows `.vbs`와 동일하게 동작하는지**
- 태블릿 목록의 `_맥` 접미사 세션들의 **출처** (이 PC 아님)
- Cursor에서 Claude Code를 쓸 때 **대화 기록이 CLI와 공유되는지**
